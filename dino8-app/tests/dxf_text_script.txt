# DXF TEXT import QC. Dino 8 never writes a native DXF TEXT entity itself
# (Text is always pre-converted to glyph-outline curves on creation - see
# AddTextCurves in cmd_annotate.cpp), so this can't be exercised via a
# Dino8-authored export/import round trip. tests/dxf_text_fixture.dxf is a
# minimal, hand-written DXF file (ASCII group-code pairs, no CAD tool
# involved) containing one real TEXT entity ("Hi" at height 5, matching the
# DWG TEXT fixture in dwg_fixture_gen.c so the same "H is one contour, i is
# two: stem + dot" expectation applies), proving ImportDxf's TEXT-to-
# glyph-outline conversion for real.
# smoke.sh substitutes @TMP@ with a scratch directory (and copies the
# fixture there) before running this script.
Open @TMP@/dxf_text_fixture.dxf
SelAll
List
