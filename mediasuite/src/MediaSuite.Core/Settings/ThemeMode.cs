namespace MediaSuite.Core.Settings;

/// <summary>Which palette the shell uses.</summary>
public enum ThemeMode
{
    /// <summary>Track the Windows "app mode" setting and follow it live.</summary>
    System,
    Light,
    Dark,
}
