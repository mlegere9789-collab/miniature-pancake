services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vitals
      POSTGRES_PASSWORD: vitals
      POSTGRES_DB: vitals
    ports:
      - "5432:5432"
    volumes:
      - vitals-postgres:/var/lib/postgresql/data

volumes:
  vitals-postgres:
